class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        if(m==0){
            nums1[0]=nums2[0];
        }
        int k=0;
        for(int i=m;i<n+m;i++){
            nums1[i]=nums2[k++];
        }
        Arrays.sort(nums1);
    }
}
